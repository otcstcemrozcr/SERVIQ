# SERVIQ — Durum 2026-05-31 / i18n Stage B

Önceki: [04-status-2026-05-31.md](04-status-2026-05-31.md). Brief: [01-master-prompt.md](01-master-prompt.md).

## Bu oturumda yapılanlar

### project-follow süreci iyileştirildi
- `current-session.md` eklendi: yeni AI oturumları için kısa proje pusulası.
- `decisions.md` eklendi: ADR benzeri karar kaydı.
- `backlog.md` eklendi: Now / Next / Later iş sırası.
- `session-template.md` eklendi: yeni AI oturumu başlangıç prompt'u.
- `README.md` yeni takip yapısını anlatacak şekilde güncellendi.

### i18n Stage B tamamlandı
- `apps/web/app/serviq/page.tsx` ana iş emri ekranı TR/EN i18n sistemine taşındı.
- Ana SERVIQ ekranına `LanguageToggle` eklendi.
- Form label'ları, tab adları, butonlar, boş durumlar, başarı/hata mesajları, material/time/signature/payment/assistant metinleri `t()` kataloğuna taşındı.
- Tarih ve para gösterimleri seçili locale'e göre formatlanacak hale getirildi.
- Material ve payment enum gösterimleri typed label helper'larıyla çevrildi.

### Deploy hazırlığı ilerledi
- `apps/api/pyproject.toml` içine Vercel FastAPI entrypoint eklendi: `[project.scripts] app = "serviq.main:app"`.
- `deploy-runbook.md` eklendi: Vercel project, Postgres, migration, env vars, deploy sırası ve smoke test adımları.
- MongoDB kapsam dışı bırakıldı; ana DB kararı PostgreSQL / Vercel Postgres olarak korundu.

## Doğrulama

- `.\node_modules\.bin\tsc.CMD --noEmit` ✅
- `.\node_modules\.bin\next.CMD build` ✅
- `python -c "from serviq.main import app; print(app.title)"` ✅
- `python -m pytest` (`apps/api` altında) ✅ 4/4

Not: `pnpm --filter @serviq/web build` PowerShell execution policy / fetch hatası verdiği için yerel binary ile doğrulandı.

## Durum

- **Phase 0:** ✅ Tamamlandı
- **i18n Stage A:** ✅ Tamamlandı
- **i18n Stage B:** ✅ Tamamlandı
- **Deploy:** ⏳ Sıradaki ana iş
- **Phase 1:** ⏳ 8 ürün/entegrasyon sorusu cevaplandıktan sonra

## Sıradaki önerilen adım

1. Vercel Postgres provider ekle.
2. `supabase/migrations/` SQL dosyalarını yeni DB'ye uygula.
3. Backend deploy: `serviq-api`.
4. Frontend deploy: `serviq-web`.
5. CORS config: frontend domain'ini backend `CORS_ORIGINS` içine ekle.
