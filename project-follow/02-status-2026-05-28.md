---
name: serviq-status-2026-05-28
description: "Snapshot of SERVIQ project state as of 2026-05-28 — repo cleanup completed, build configs added, Phase 0 plan delivered and awaiting approval."
metadata: 
  node_type: memory
  type: project
  originSessionId: 20731a82-269b-4204-ab84-a181532ed044
---

Snapshot date: **2026-05-28**. State after the session where the user asked to clean up Codex's confusion between OpenOps AI and SERVIQ repos and to deliver the [[serviq-master-prompt]] analysis.

**Why:** User explicitly asked to save the last conversation state on this date so the next session can resume without re-deriving context.

**How to apply:** When resuming SERVIQ work, treat this as the last known state and verify with `git log` + file checks before acting (memory may have drifted).

---

## Repo layout (resolved)

- **Active repo:** `C:\Users\ozcir\Desktop\ServiQ` — remote `origin` → `https://github.com/otcstcemrozcr/SERVIQ.git`, branch `main`, working tree clean, fully pushed.
- **Old seed (to delete):** `C:\Users\ozcir\Desktop\ServiQ-seed-eski` — only 1 commit, garbage `originv1` remote. User approved deletion but it could not be removed because this Claude Code session is rooted in that folder. Delete manually after closing the session, OR start the next Claude session from `Desktop\ServiQ`.
- **OpenOps AI repo:** `C:\Users\ozcir\Desktop\openops-ai` — SERVIQ already extracted (commit `22a07e7 Remove SERVIQ module — extracted to separate repo`). No further cleanup needed there.

## Commits delivered this session (on `main`, pushed)

1. `8698b28` Add FastAPI shell: main, auth, rbac, db (already existed locally, pushed)
2. `7fc05ad` Rename openops_api package to serviq; flatten module
3. `40b3257` Add frontend, migrations, tests and PDF module
4. `9bbcabe` Add build config: pyproject/requirements, package.json/tsconfig/next, .env.example, pnpm workspace

## Files added in commit 9bbcabe

- `apps/api/pyproject.toml`, `apps/api/requirements.txt` — FastAPI + SQLAlchemy + psycopg + PyJWT
- `apps/web/package.json`, `apps/web/tsconfig.json`, `apps/web/next.config.mjs`
- root `package.json` (pnpm workspace), `pnpm-workspace.yaml`
- `.env.example` — covers APP_ENV, OPENOPS_API_KEY (legacy name still used in code), DOCS_TOKEN, CORS_ORIGINS, RATE_LIMIT_PER_MINUTE, MAX_BODY_BYTES, DATABASE_URL, SUPABASE_*, NEXT_PUBLIC_*

## Backend/frontend contract mismatches (open bugs)

These were found during analysis and are NOT yet fixed — Phase 0 will close them:

1. **`scheduled_for`** — column exists in DB migration `20260525000001_serviq_schedule.sql` and ORM `db/models.py`, but `models.py` `ServiqWorkOrder`, `schemas.py` `WorkOrderCreate/Update/Out`, and the route serializer all omit it. Frontend `lib/api.ts` and tests both expect it.
2. **`GET /serviq/work-orders/{id}/report.pdf`** — frontend `downloadServiqReportPdf()` and `test_serviq_pdf_and_assistant_summary` call this; route does not exist in `apps/api/serviq/routes/serviq.py`. `pdf.py` (`build_service_report_pdf`) is implemented and unused.
3. **`POST /serviq/ai/service-summary`** — currently returns 501. Frontend `getServiqAssistantSummary()` and test both expect a structured `ServiqAssistantSummary` JSON.
4. **Naming legacy:** code uses `OPENOPS_API_KEY` and `OPENOPS_SERVICE_USER_ID`. User hasn't decided on rename vs. alias yet.

## Phase 0 plan (awaiting approval)

Delivered as the 12-section analysis in the last assistant turn. Phase 0 deliverables:

1. Add `ServiqWorkOrder.scheduled_for` to ORM + Pydantic schemas + route serializer.
2. Implement `GET /serviq/work-orders/{id}/report.pdf` using existing `pdf.build_service_report_pdf`.
3. Implement `POST /serviq/ai/service-summary` as deterministic rule-based assistant (no LLM yet — grounded in work-order data).
4. Get existing pytest suite green.
5. Update `.env.example` for SERVIQ rebranding (decision pending on alias vs. rename).

## Open questions waiting on user

Before Phase 5 (ERP) and Phase 6 (Payments) can be scoped, user must answer:

1. **Which ERP first?** Logo / Mikro / Netsis / SAP?
2. **Turkish e-fatura/e-arşiv provider?** eLogo / Foriba / Nilvera / GİB?
3. **Payment provider commitment?** Iyzico definite, or also Stripe/PayTR?
4. **Technician auth flow** — device login vs. one-time code/QR? (affects offline)
5. **Languages** — TR+EN simultaneously, or TR only?
6. **PWA vs. React Native** for the technician mobile experience?
7. **Signature/photo storage** — keep base64 (current), or move to Supabase Storage?
8. **Env naming** — rename `OPENOPS_*` to `SERVIQ_*` (with alias), or leave?

## Existing architecture strengths to preserve

- Adapter Protocols already in place: `erp.base`, `email`, `payments`, `ai` — no SAP/Iyzico lock-in.
- Multi-tenant clean: `org_id` + `X-Org-ID` header + Supabase RLS + `require_role`.
- Workflow rules isolated in `workflow.py` (separation from HTTP).
- Lightweight zero-dependency PDF renderer (will need Turkish character/TTF support later).

## Known weaknesses

- `routes/serviq.py` ~370 lines, single file
- `web/app/serviq/page.tsx` ~1100 lines, single file with inline styles
- `lib/api.ts` ~420 lines mixing OpenOps and SERVIQ
- No offline-first scaffolding yet (only localStorage for auth)
- Tab navigation is desktop-shaped; not optimized for technician one-handed mobile use
- PDF Helvetica/Latin-1 only — Turkish chars will break

See [[serviq-master-prompt]] for the full vision this state is being built toward.
