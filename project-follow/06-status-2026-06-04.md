# SERVIQ — Durum 2026-06-04 / Production DB + Deploy

Onceki: [05-status-2026-05-31.md](05-status-2026-05-31.md). Brief: [01-master-prompt.md](01-master-prompt.md).

## Ozet

Production deploy tamamlandi. SERVIQ artik Vercel uzerinde iki proje olarak calisiyor:

- Backend: `serviq-api` -> `https://serviq-api.vercel.app`
- Frontend: `serviq-web` -> `https://serviq-web.vercel.app/serviq`
- Database: Vercel Marketplace uzerinden Neon resource `serviq-postgres`

## Tamamlananlar

- Neon marketplace resource olusturuldu ve `serviq-api` projesine baglandi.
- Production `DATABASE_URL` env var'i Vercel'e geldi.
- DB semasi mevcut SQLAlchemy modellerinden olusturuldu.
- Backend env vars ayarlandi:
  - `APP_ENV=production`
  - `OPENOPS_API_KEY`
  - `OPENOPS_SERVICE_USER_ID=service`
  - `DOCS_TOKEN`
  - `CORS_ORIGINS=https://serviq-web.vercel.app`
- Frontend env vars ayarlandi:
  - `NEXT_PUBLIC_API_URL=https://serviq-api.vercel.app`
- `serviq-api` ve `serviq-web` production deploy edildi.
- Root directory ayarlari dogrulandi:
  - `serviq-api`: `apps/api`
  - `serviq-web`: `apps/web`
- Backend DB driver uyumlulugu duzeltildi: Neon `postgresql://` URL'i `postgresql+psycopg://` olarak normalize ediliyor.

## Validation

- `python -m pytest tests/unit/test_serviq_api.py` -> 7/7 passed (`PYTHONPATH=apps/api` ile).
- `GET https://serviq-api.vercel.app/health` -> `{"status":"ok"}`.
- Authenticated `GET /serviq/work-orders` -> `200`, liste dondu.
- Authenticated `POST /serviq/work-orders` -> `201 Created`, test is emri Neon DB'ye yazildi.

## Notlar

- Supabase migration SQL'leri Neon production DB'ye dogrudan uygulanmadi; Supabase auth/RLS bagimliliklari var.
- Production semasi su an SQLAlchemy modelleri ile hizali.
- Gelecekte kontrollu migration icin Alembic veya benzeri bir arac eklenmeli.
- API key gerektiren live mod korunuyor; API key yoksa frontend demo/local mode ile calisabiliyor.

## Siradaki onerilen isler

1. Work order detail page iyilestirmeleri.
2. Mobile technician workflow.
3. SignaturePad.
4. PaymentModal.
5. MaterialUsageTable.
6. TimeTrackingForm.
