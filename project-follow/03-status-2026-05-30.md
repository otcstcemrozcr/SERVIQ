# SERVIQ — Durum 2026-05-30

Önceki: [02-status-2026-05-28.md](02-status-2026-05-28.md). Brief: [01-master-prompt.md](01-master-prompt.md).

## Bu oturumda yapılanlar

### Hafıza/klasör düzeni
- Eski oturumun hafıza dosyaları yanlış proje yoluna (`…ServiQ-seed-eski\memory`) yazılmıştı; aktif proje yoluna (`…ServiQ\memory`) taşındı.
- `project-follow/` klasörü oluşturuldu (repo içinde, insan-okur kopya). Commit `0f0d45e`.

### Phase 0 tamamlandı — commit `e5af2e0` (push'lu)
4 kontrat uyumsuzluğu kapandı, pytest yeşil (4/4):

1. **`scheduled_for`** — `ServiqWorkOrder` ORM kolonu + `WorkOrderCreate/Update/Out` şemaları + route serializer + create'e eklendi. DB migration'ında zaten vardı; API düşürüyordu.
2. **`GET /serviq/work-orders/{id}/report.pdf`** — eklendi; kullanılmayan `pdf.build_service_report_pdf` bağlandı. `Content-Type: application/pdf`, `Content-Disposition: inline`.
3. **`POST /serviq/ai/service-summary`** — 501 yerine deterministik, kural-tabanlı, work-order datasına dayalı özet (`ai.build_service_summary`). LLM yok. Dönen şekil frontend `ServiqAssistantSummary` ile birebir: `summary, next_actions, missing, totals{materials_count,labor_hours,travel_hours,waiting_hours}, customer_email_draft, erp_notification_text`.
4. **pytest green** — Asıl kırık neden: `JSONB` (Postgres'e özel) SQLite test DB'sinde derlenmiyordu. `JSONB().with_variant(JSON(), "sqlite")` ile çözüldü (`models.py`'de `JSONColumn`). Prod'da JSONB, testte JSON.

### Karar verilen
- **Madde 5 (env rebranding):** `OPENOPS_API_KEY` / `OPENOPS_SERVICE_USER_ID` **şimdilik dokunulmadı** (kullanıcı kararı). Rename sonraya ertelendi.

## Değişen dosyalar (Phase 0)
`apps/api/serviq/`: `models.py`, `schemas.py`, `routes/serviq.py`, `ai.py` (+164/−11). Frontend ve migration'a dokunulmadı — frontend zaten bu kontratı bekliyordu.

## Hâlâ açık (sıradaki oturuma)
Phase 5 (ERP) ve Phase 6 (Payments) scope'lanmadan önce kullanıcı cevabı bekleyen sorular (02-status'tan devam):
1. Hangi ERP önce? (Logo / Mikro / Netsis / SAP)
2. e-fatura/e-arşiv sağlayıcı? (eLogo / Foriba / Nilvera / GİB)
3. Ödeme sağlayıcı? (Iyzico kesin mi, + Stripe/PayTR?)
4. Teknisyen auth — cihaz login vs. tek kullanımlık kod/QR?
5. Dil — TR+EN eşzamanlı mı, TR only mı?
6. PWA vs. React Native?
7. İmza/foto storage — base64 (mevcut) vs. Supabase Storage?
8. Env rename `OPENOPS_*` → `SERVIQ_*` — ne zaman? (Phase 0'da ertelendi)

## Bilinen teknik borç (engel değil)
- Pydantic v1 kalıntıları: `orm_mode` (→ `from_attributes`), `.dict()` (→ `model_dump()`), `class Config` (→ `ConfigDict`). Testlerde sadece deprecation warning.
- `datetime.utcnow()` deprecation (Py3.14).
- PDF hâlâ Helvetica/Latin-1 — Türkçe karakter bozulur (Phase 6/rapor işinde TTF gerekecek).
- `routes/serviq.py` tek dosya, `web/app/serviq/page.tsx` ~1100 satır, `lib/api.ts` ~420 satır — bölünmeyi bekliyor.
