# SERVIQ — Durum 2026-05-31

Önceki: [03-status-2026-05-30.md](03-status-2026-05-30.md). Brief: [01-master-prompt.md](01-master-prompt.md).

## Bu oturumda yapılanlar

### Deploy planlaması başlandı
- Vercel kimlik doğrulaması ✅ (team: otcstcemrozcrs-projects)
- FastAPI-on-Vercel araştırması: `pyproject.toml` entrypoint yeterli, özel wrapper gerekmiyor
- **Veritabanı kararı:** Vercel Postgres (Neon-based, ücretsiz, serverless, haftalık duraklamaz)
- Backend scaffold: `[tool.vercel] entrypoint = "serviq.main:app"` + `requirements.txt`

### i18n Stage A tamamlandı ve push'landı (commit `b8de224`)
- `lib/i18n.tsx` — typed katalog, LocaleProvider, useT(), useFormatters(), LanguageToggle
- `app/layout.tsx` — root layout + LocaleProvider
- `dashboard/page.tsx` — tamamen TR/EN, 40+ string migre edildi
- `components/ui.tsx` — Card `id` prop eklendi
- Doğrulama: tsc temiz, `next build` başarılı

## Yapılması gerekenleri (açık kalan)

### İmmediata (deploy için)
1. **Vercel Postgres sağlayıcı:** Marketplace'ten ekle → DATABASE_URL otomatik gelir
2. **Migrasyonları uygula:** `supabase/migrations/` SQL'leri provizyonlandı DB'ye çalıştır
3. **Backend Vercel scaffolding:** `pyproject.toml`'a `[tool.vercel]` entrypoint ekle
4. **Deploy backend:** `serviq-api` projesi, env vars: `DATABASE_URL`, `OPENOPS_API_KEY`, `APP_ENV=production`, `CORS_ORIGINS`
5. **Deploy frontend:** `serviq-web` projesi, env var: `NEXT_PUBLIC_API_URL` (backend URL)
6. **CORS konfigürasyonu:** frontend domain'i backend'in `CORS_ORIGINS`'ine ekle

### Stage B (sıradaki oturum)
- **i18n Stage B:** büyük `page.tsx` (1108 satır) migrasyonu — `t()`, `fmt.*()`, dil toggle

### Açık kalan (8 soru — ERP/Payments/Auth faz öncesi)
1. Hangi ERP? (Logo / Mikro / Netsis / SAP)
2. e-fatura sağlayıcı? (eLogo / Foriba / Nilvera / GİB)
3. Ödeme sağlayıcı? (Iyzico kesin, + Stripe/PayTR?)
4. Teknisyen auth? (cihaz login vs. QR/tek-kod)
5. PWA vs. React Native?
6. İmza/foto storage? (base64 vs. Supabase Storage)
7. Env rename `OPENOPS_*`? (şimdilik ertelendi)

(Dil kararı: TR+EN ✅)

## Durum
- **Phase 0:** ✅ Tamamlandı, pytest 4/4 yeşil
- **i18n Stage A:** ✅ Tamamlandı, tsc + build doğrulanmış
- **Deploy:** ⏳ Planlandı, DB + Vercel setup awaiting
- **i18n Stage B:** ⏳ Sırada
- **Phase 1 (sonra):** ⏳ 8 soru cevaplandıktan sonra

## Teknik borç (engel değil)
- Pydantic v1 (deprecation warning'ler, fonksiyonel)
- `datetime.utcnow()` (Py3.14)
- PDF Latin-1 (Türkçe karakter bozulur, Phase 6'da)
- Büyük tek dosyalar (`routes/serviq.py`, `page.tsx`, `lib/api.ts`)
